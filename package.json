{
  "dependencies": {
    "axios": "^1.8.4",
    "gh-pages": "^6.3.0",
    "jquery-mask-plugin": "1.14.16",
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
      "predeploy": "npm run build",
      "deploy": "gh-pages -d build"
  }

}
